%YAML 1.1
---
$schema: http://stsci.edu/schemas/asdf/asdf-schema-1.0.0
id: "http://stsci.edu/schemas/roman_datamodel/variance.schema"
type: object
properties:
    var_poisson:
      title: variance due to poisson noise
      max_ndim: 3
      default: 0.0
      datatype: float32
    var_rnoise:
      title: variance due to read noise
      max_ndim: 3
      default: 0.0
      datatype: float32
    var_flat:
      title: variance due to flat field
      max_ndim: 3
      default: 0.0
      datatype: float32
