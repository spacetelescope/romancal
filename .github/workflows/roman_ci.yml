name: test

on:
  push:
    branches:
      - main
      - '*x'
    tags:
      - '*'
  pull_request:
    branches:
      - main
  schedule:
    # Weekly Monday 9AM build
    - cron: "0 9 * * 1"
  workflow_dispatch:
    inputs:
      crds_context:
        description: CRDS context (leave blank for latest)
        type: string
        required: false
        default: ''
      crds_server:
        description: CRDS server
        type: string
        required: false
        default: https://roman-crds.stsci.edu

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ inputs.crds_context }}
  cancel-in-progress: true

jobs:
  check:
    uses: OpenAstronomy/github-actions-workflows/.github/workflows/tox.yml@v1
    with:
      envs: |
        - linux: check-dependencies
  webbpsf_data_cache:
    name: download WebbPSF data
    runs-on: ubuntu-latest
    steps:
      - run: wget https://stsci.box.com/shared/static/3hzmbarac5yxjt6x7gn17vz02k7c8z1d.gz -O ${{ runner.temp }}/webbpsf-data.tar.gz
      - run: mkdir ${{ runner.temp }}/data/
      - run: tar -xzvf ${{ runner.temp }}/webbpsf-data.tar.gz -C ${{ runner.temp }}/data/
    outputs:
      path: ${{ runner.temp }}/data/webbpsf-data/
  latest_crds_contexts:
    uses: ./.github/workflows/contexts.yml
  crds_context:
    needs: [ latest_crds_contexts ]
    runs-on: ubuntu-latest
    steps:
      - id: context
        run: echo context=${{ github.event_name == 'workflow_dispatch' && (inputs.crds_context != '' && inputs.crds_context || needs.latest_crds_contexts.outputs.roman) || needs.latest_crds_contexts.outputs.roman }} >> $GITHUB_OUTPUT
    outputs:
      context: ${{ steps.context.outputs.context }}
  test:
    uses: OpenAstronomy/github-actions-workflows/.github/workflows/tox.yml@v1
    needs: [ webbpsf_data_cache, crds_context ]
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
    with:
      setenv: |
        WEBBPSF_PATH: ${{ needs.webbpsf_data_cache.outputs.path }}
        CRDS_PATH: /tmp/data/crds_cache/
        CRDS_SERVER_URL: ${{ github.event_name == 'workflow_dispatch' && inputs.crds_server || 'https://roman-crds.stsci.edu' }}
        CRDS_CONTEXT: ${{ needs.crds_context.outputs.context }}
        CRDS_CLIENT_RETRY_COUNT: 3
        CRDS_CLIENT_RETRY_DELAY_SECONDS: 20
        DD_SERVICE: romancal
        DD_ENV: ci
        DD_GIT_REPOSITORY_URL: ${{ github.repositoryUrl }}
        DD_GIT_COMMIT_SHA: ${{ github.sha }}
        DD_GIT_BRANCH: ${{ github.ref_name }}
      cache-path: /tmp/data/
      cache-key: data-${{ needs.crds_context.outputs.context }}
      envs: |
        - linux: py311-oldestdeps-webbpsf-cov
          pytest-results-summary: true
        - linux: py311-webbpsf-nolegacypath
          pytest-results-summary: true
        - linux: py312-ddtrace-webbpsf
          pytest-results-summary: true
        - macos: py312-ddtrace-webbpsf
          pytest-results-summary: true
        - linux: py312-webbpsf-cov
          coverage: codecov
          pytest-results-summary: true
        - linux: py313-webbpsf
          pytest-results-summary: true
